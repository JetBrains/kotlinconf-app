product:
  type: kmp/lib
  platforms: [ wasmJs, js ]

apply:
  - ../../compose.module-template.yaml
  - ../../metro.module-template.yaml

# TODO: Support for apps from kotlinconf-app.js

settings:
  compose:
    resources:
      packageName: org.jetbrains.kotlinconf.web.generated.resources

dependencies:
  - ../shared
  - $compose.components.resources